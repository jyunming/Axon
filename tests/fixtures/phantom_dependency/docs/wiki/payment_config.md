# Payment Configuration

Region activity for payments is fetched dynamically from the `GlobalSettings` API. The frontend checks this by parsing the `vendor_status` key returned by the API for the given region.
