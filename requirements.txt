# Axon - Requirements
# Python 3.10+ required

# Core dependencies
numpy>=1.20.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0
pandas>=2.0.0
Pillow>=10.0.0
tqdm>=4.66.0
rank_bm25>=0.2.2
# DEPRECATED: only needed by the legacy Streamlit UI (axon-ui), which the
# docker-compose `axon-ui` service still runs. The maintained web GUI is served
# by axon-api at /gui/ and needs no extra dependency.
streamlit>=1.30.0
httpx>=0.25.0
# Upper-bounded: mcp 2.0.0 removed `mcp.server.fastmcp` (see pyproject.toml).
mcp>=1.0.0,<2.0.0
fastapi>=0.100.0
uvicorn>=0.22.0
prompt_toolkit>=3.0.0
rich>=13.0.0
tiktoken>=0.7.0

# Vector Stores
# turboquantdb (tqdb) is the default. chroma/qdrant are pyproject extras kept
# here so pip-audit still covers them; lancedb moved to the [lancedb] extra in
# 0.5.0 and is no longer installed by default.
tqdb>=0.7.0
chromadb>=0.4.0
qdrant-client>=1.7.0

# Embeddings
# fastembed is the DEFAULT embedding provider since 0.4.6. It was previously
# commented out here, so this file installed the torch stack and not the
# provider the shipped config actually uses.
fastembed>=0.1.0,<1.0.0

# LLM Providers
ollama>=0.1.0
openai>=1.0.0
google-genai>=1.4.0

# NOTE: sentence-transformers and tf-keras were removed here in 0.5.0. They are
# only reachable via embedding.provider=sentence_transformers or
# rerank.provider=cross-encoder (both non-default), and together pulled ~2.1 GB
# of torch + TensorFlow into every install and Docker image.
#   pip install 'axon-rag[sentence-transformers]'

# Document loaders
python-docx>=1.0.0
python-pptx>=0.6.18
pymupdf>=1.24.0
pypdf>=4.0.0
openpyxl>=3.1.0
pyarrow>=10.0.0

# Optional: For future vLLM support
# vllm>=0.2.0

# Optional: For async support
# aiohttp>=3.8.0

# Optional: For RAGAS evaluation
# ragas>=0.1.0
# langchain-community>=0.0.20
# datasets>=2.14.0

